import {Activity,AlertTriangle,ClipboardCheck} from "lucide-react";
import {C} from "../design/tokens";
export default function Header({active,high,decisions,time}){const pill="flex items-center gap-1.5 rounded-full px-3 py-1.5 text-xs font-extrabold";return <header className="flex h-16 shrink-0 items-center gap-4 px-5 text-white" style={{background:C.primary}}><div><h1 className="text-xl font-black">ORBIT Bus Dashboard</h1><p className="text-xs text-white/60">See how UTM buses respond to student demand</p></div><div className="ml-auto flex items-center gap-2"><span key={`active-${active}`} className={`${pill} state-change`} style={{background:C.success}}><Activity size={14}/>{active} on the road</span><span key={`high-${high}`} className={`${pill} state-change`} style={{background:high?C.orange:"rgba(255,255,255,.14)"}}><AlertTriangle size={14}/>{high?`${high} busy stop`:"no busy stops"}</span><span key={`updates-${decisions}`} className={`${pill} state-change`} style={{background:"rgba(255,255,255,.14)"}}><ClipboardCheck size={14}/>{decisions} updates</span><time key={`time-${time}`} aria-label="Demo time" className="state-change ml-2 font-mono text-sm text-white/60">{time}</time></div></header>}
