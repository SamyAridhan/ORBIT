import {X} from "lucide-react";
import {C} from "../design/tokens";
export default function OverrideModal({bus,onClose,onConfirm}){if(!bus)return null;return <div role="dialog" aria-modal="true" aria-label={`Override Bus ${bus.id}`} className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/60"><div className="w-[440px] rounded-2xl border p-6 shadow-2xl" style={{background:C.card,borderColor:C.border}}><div className="flex items-start"><div><h2 className="text-xl font-black" style={{color:C.text}}>Override Bus {bus.id}</h2><p className="mt-1 text-sm" style={{color:C.textSec}}>Cancel its active timing intervention and return to planned timing.</p></div><button aria-label="Close override" onClick={onClose} className="ml-auto p-1" style={{color:C.textMuted}}><X/></button></div><div className="mt-5 rounded-xl p-4 text-sm leading-relaxed" style={{background:C.accentLight,color:"#92400E"}}>This simulates publishing a timing command to <code>admin/override</code>. Bus {bus.id} remains on Corridor {bus.corridor}; its route never changes.</div><div className="mt-5 flex justify-end gap-2"><button onClick={onClose} className="rounded-xl border px-5 py-3 text-sm font-bold" style={{borderColor:C.border,color:C.text}}>Cancel</button><button onClick={onConfirm} className="rounded-xl px-5 py-3 text-sm font-bold text-white" style={{background:C.red}}>Confirm override</button></div></div></div>}
