import {LockKeyhole} from "lucide-react";
import {C} from "../design/tokens";
import {DEMAND_CFG} from "../data/mockData";
export default function StopCard({stop,focused=false}){const cfg=DEMAND_CFG[stop.level],semantic=stop.level==="CRITICAL"?C.red:stop.level==="HIGH"?C.orange:C.border;return <article aria-label={`Stop ${stop.name}`} aria-current={focused?"step":undefined} className={`rounded-xl border-2 p-3 transition-all ${focused?"shadow-md":""}`} style={{background:C.card,borderColor:focused&&semantic===C.border?C.primaryLight:semantic}}><div className="flex items-center gap-2"><h3 className="text-base font-extrabold" style={{color:C.text}}>{stop.name}</h3><span className="rounded-md px-2 py-0.5 text-xs font-bold" style={{background:C.border,color:C.textSec}}>Bus {stop.corridor}</span>{focused&&<span className="text-[10px] font-extrabold uppercase tracking-wide" style={{color:C.primary}}>Current focus</span>}{stop.claimedBy&&<span className="ml-auto flex items-center gap-1 text-xs font-bold" style={{color:C.purple}}><LockKeyhole size={15}/>Claimed by {stop.claimedBy}</span>}</div><div className="mt-2 flex items-end gap-3"><strong className="text-4xl font-black leading-none" style={{color:stop.level==="HIGH"?C.orange:stop.level==="CRITICAL"?C.red:C.text}}>{stop.queue}</strong><div><p className="text-sm font-semibold" style={{color:C.textSec}}>people waiting</p><span className="mt-1 inline-flex items-center gap-1 rounded-full px-2 py-1 text-xs font-extrabold" style={{background:cfg.bg,color:cfg.text}}><span className={`h-2 w-2 rounded-full ${stop.level==="CRITICAL"?"critical-pulse":""}`} style={{background:cfg.color}}/>{stop.level}</span></div></div><p className="mt-2 text-xs" style={{color:C.textMuted}}>Last bus {stop.lastBus===0?"just now":`${stop.lastBus} min ago`}</p></article>}
